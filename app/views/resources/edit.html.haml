.row
  .span8
    = form_for @resource do |f|
      = f.text_field :name, :class => 'big'
      %div
        = f.submit 'Update', :class => 'btn btn-primary'

  .span4
    - @properties = @resource.properties
    = render :file => 'properties/index'
